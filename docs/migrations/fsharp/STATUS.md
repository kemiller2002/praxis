# ROS F# migration status

Last updated: 2026-09-11 (post `DF-ROS-2026-A031`, which adds an additive `ros-fs` F# launcher to the greenfield starter template). `starter/greenfield/ros` (what other projects get via `npx ros-bootstrap init`) still runs Node, unchanged; redirecting it, per a near-miss caught while building `DF-ROS-2026-A031`, would invalidate this whole migration's differential-test methodology (every such test spawns a bootstrapped project's `ros` as its Node baseline), so it is deliberately not attempted.

| Category | Current state |
|---|---|
| Baseline | clean SHA tagged and pushed; active continuation branch `migration/ros-work-verified-context` started from `ff47c69` |
| Discovery | complete at T1; 28 baseline execution/config/platform units plus six executable test units; MIG-06 added one inventoried adapter |
| Semantic decomposition | complete at T2; map and six feature manifests added |
| Architecture | accepted for staged shadowing in `DF-ROS-2026-A027`; challenged before treatment |
| F# implementation | MIG-03/04 and MIG-06 complete; MIG-07 includes typed live/backlog decisions, pure item/context/promotion planning, post-plan batch evidence verification, telemetry execution-ID result-feedback, explicit contracts, and shadow planning diagnostics |
| Persistence migration | MIG-05 complete: bounded registry, live-work, and backlog recovery units plus atomic per-execution telemetry files and recoverable execution/context linking; no cross-store/global transaction claim |
| Git migration | MIG-06 complete: F# owns the typed status contract; production work/telemetry effects share one contract-compatible Node adapter pending the distribution decision |
| Work migration | MIG-07 decision, item/context/backlog-promotion planning, typed evidence-observation, telemetry execution-ID resolution, work-attribution validation (`work validate`, mirroring production `workFindings`), backlog-queue validation (`work backlog-validate`, mirroring production `queueFindings`), and eight real work-lifecycle effects (`work backlog-transition`, `work capture`, `work update`, `work attach`, `work start`, `work resume`, `work block`, `work complete`) sub-slices complete — every backlog-only Node command and all four live-work transitions now have real F# effect parity. MIG-08's first eight increments add two real telemetry-producer commands (`telemetry adapters`, `telemetry show`), real telemetry lifecycle bookkeeping (`recordTelemetryLifecycle`, now recorded by `work resume`/`work block`), real telemetry summary aggregation (`telemetry summary`), four write-path telemetry-producer commands (`telemetry finalize`, reusing the existing `finalizeOne` mutation behind production's exact target-resolution semantics excluding `--input` adapter ingestion; `telemetry record`, a content-addressed explicit metric append with production's exact `activeOnly` target resolution and validation ordering; `telemetry ingest`, restricted to the generic adapter, with real raw-payload redaction, the byte-budget retention policy, and `(metricId, providerField)`-keyed capability upsert; and `telemetry start`, which recovers-or-creates a telemetry execution outside any state transition, linking it via a narrow direct context read-modify-write), and `telemetry classify` (a thin wrapper over `telemetry ingest`'s own mutation path); MIG-08's ninth increment adds `adapter call` (`DF-ROS-2026-A007`'s external work-system adapter conformance test, work-lifecycle-owned rather than telemetry, but tracked in this same remaining-scope inventory), via a new `Ros.Domain.Work.AdapterContract.decide`/`Ros.Infrastructure.Work.FileAdapterRepository`; the tenth increment adds `adapter publish` (the paired event-republish command, same module). `work resume`'s `parentExecutionId` linkage, once scoped as a further candidate slice, turned out not to be missing at all: production's own computed value is silently discarded by a confirmed `discoverIdentity(options.identity ?? options)` object-merge defect, so the F# port now implements the evidently-intended behavior instead of replicating the bug -- this migration's one deliberate divergence from production, justified because Node is being deprecated rather than patched. MIG-08's eleventh increment adds `telemetry ingest --adapter openai-codex` (`FileTelemetryFinalizationRepository.adaptOpenAICodex`), the first real provider-specific field mapping beyond `generic`, confirming the shared generic-ingest mutation pipeline is fully adapter-agnostic. The twelfth increment adds the three hook adapters (`anthropic-claude-hook`, `google-gemini-hook`, `github-copilot-hook`), sharing one `adaptHook` parameterized only by identity provider/runtime: unlike `adaptOpenAICodex`'s fixed six-field capability declaration, `adaptHook`'s capabilities are derived 1:1 from whichever of its independently-triggered (non-exclusive, regex-matched) metrics actually fired for a given hook payload, with a single `runtime.{hookName}` event per call regardless of how many metrics fired. The thirteenth increment adds `telemetry ingest --adapter anthropic-claude-statusline` (`adaptClaudeStatusline`): unlike every prior adapter, its input is a single snapshot rather than an event stream -- three top-level fields (`context.window_size`/`context.utilization`/`cost.session_cumulative`) and four `current_usage` token fields each always get a capability declaration (present or not), with `cost.session_cumulative` the one field whose present-value capability status becomes `"estimated"` rather than `"supported-observed"` (a statusline snapshot cannot directly observe session cost, only estimate it); this adapter emits no events of its own at all. The fourteenth and final adapter-mapping increment adds the OTel adapter family (`anthropic-claude-otel`, `google-gemini-otel`, `github-copilot-otel`, `otel-json`), all sharing one `adaptOtel`: unlike every prior adapter, it has no fixed field schema at all -- every field is looked up across a per-record set of "nested candidates" (the record itself, `attributes`, `resource.attributes`, `body`, `dataPoint.attributes`, in that fixed order), matching the range of shapes real OTLP JSON and Gemini CLI's own metric events actually carry. `identity.provider`/`runtime`/`model`/`sessionId` update per record as new values are discovered (last one wins across the stream), seeded from each adapter name's own provider/runtime pair (`"unknown"`/`"unknown"` for the bare `otel-json` name, matching production's own unset default). Like `adaptHook`, capabilities are declared 1:1 from whichever metrics actually fired -- no "recognized but unavailable" declarations. `runtimeTimestamp`, production's own numeric-magnitude heuristic for telling nanosecond/millisecond/second epoch values apart (or passing an already-ISO string through verbatim), is ported for the realistic timestamp shapes OTel exports carry. This closes MIG-08's telemetry-ingest adapter inventory: every name in `Ros.Domain.Telemetry.TelemetryAdapters.all` now dispatches to a real F# adaptation function, so `ingestTarget`'s now-permanently-unreachable "not yet supported by this CLI" rejection branch was retired. The fifteenth and final MIG-08 increment closes the one remaining gap: `telemetry start`'s own `--execution-id` and its 11 identity-override flags (`--provider`/`--model`/`--model-version`/`--runtime`/`--runtime-version`/`--session`/`--conversation`/`--run`/`--agent`/`--subagent`/`--parent-execution`), rejected outright until now. `--execution-id` needed no new decision logic at all: `Ros.Domain.Telemetry.ExecutionLinkRecovery.decide`'s pure `RequestedExecutionId` handling (already shipped, already tested) was simply threaded through from the CLI for the first time, recovering a matching detached candidate, rejecting a non-matching one with production's exact `; rerun with --execution-id ...` message when other candidates exist, or flowing through to become the newly created execution's own id when none do. The 11 identity flags reuse `Ros.Domain.Telemetry.Identity.discover`'s already-complete override handling (built earlier in this migration and never missing any field); `FileTelemetryExecutionRepository.CreateExecutionRequest` gained `ExecutionId`/`IdentityOverrides` fields (folding the pre-existing standalone `ParentExecutionId` field into the latter, since it is one of the same 11), and `createExecution` now accepts an explicit execution id and merges the full override set over environment-discovered identity, mirroring production's own `options.executionId ?? executionId()` and `discoverIdentity(options.identity ?? options)` exactly. This closes MIG-08's own command-surface scope entirely -- every command and adapter name production's own CLI exposes for execution/telemetry now has real F# effect parity. A ninth work-lifecycle increment (`EV-ROS-2026-A046`'s finding that `work`/`work list`, `work show`, `work context`, and `status` were never assigned to MIG-07/MIG-08's own scope at all) closes the smallest of those four: `work context [ID]` (`FileWorkContextRepository.readContextView`), reusing the pre-existing `WorkTransition.allowedActions`/`FileWorkConfigRepository.readCompletionEvidence` domain logic with no new decision code at all, and requiring no lock. A tenth work-lifecycle increment closes the second: `work`/`work list`/`work show [ID]` (`FileWorkListRepository.readListView`/`readShowView`) port production's `mergedWorkView`/`showWork` at full fidelity via a new `Ros.Domain.Work.WorkListView.mergedRows` (reusing `QueuePresentation.effectiveStatus`, deliberately kept separate from that same module's narrower five-field `queue.md`-only projection) and a new `BacklogTransition.allowedActions` lookup table mirroring production's `BACKLOG_TRANSITIONS`; `--tag`/`--status` filtering is not yet ported. `status` is now the one remaining row `EV-ROS-2026-A046` found unassigned. A 24th MIG-08 increment ported `telemetryFindings` (`Ros.Domain.Telemetry.TelemetryValidation`/`Ros.Infrastructure.Work.FileTelemetryValidationRepository`, exposed as `ros-fs telemetry validate`) -- the telemetry contributor to production's combined `validate` findings, the largest single validator in this migration (~80 independent checks). Unlike every other validator here, it crosses a wide typed boundary (a new `Ros.Domain.Telemetry.Validation` type family: `FieldPresence`/`IdentityField`/`ParsedItem` closed unions) rather than a small loosely-typed record, per SDE-DOCTRINE-003's Tier 2 (decides legality)/Tier 4 (reports structural facts) split. An eleventh work-lifecycle increment then unified `validate` itself: all five contributors (artifact findings, registry staleness, `workFindings`, `queueFindings`, `telemetryFindings`) already had a real F# equivalent, so `ros-fs validate [--json]` (`Ros.Cli.Program.runValidateUnified`) is pure CLI-layer orchestration -- filtering `checkRegistries`'s result to avoid double-counting parse findings, and adding the one missing `field = "work_items"` repair-message branch to the shared `FindingContract`. `status` is now unblocked. A twelfth work-lifecycle increment then ported `status` itself: production's own `statusView` composes `contextView`/the unified `validate` findings/`showTelemetry`'s execution read, all already real, so `Ros.Cli.Program.runStatus` adds zero new Domain/Infrastructure code -- closing `EV-ROS-2026-A046`'s full four-row inventory (`work`/`work list`, `work show`, `work context`, `status`) |
| Production command switch | `DF-ROS-2026-A028`'s three-phase plan is now closed for this repository. Phase A (`EV-ROS-2026-A047`: all 26 rows "Full parity"), Phase B (`DF-ROS-2026-A029`/`EV-ROS-2026-A048`: self-contained single-file binaries via GitHub Releases, `ros-fs`), and now Phase C (`DF-ROS-2026-A030`): this repository's own `./ros` execs the F# CLI directly (framework-dependent `dotnet .../ros-fs.dll`, since this repo already requires the .NET SDK to build/test itself). `tools/ros_cli.mjs` remains in the repository, fully tested, as the decision's rollback path. Closing this gap required three small, additive F# CLI-surface fixes (`add` as a top-level command, `--tag`/`--status` filtering on `work`/`work list` so bare `work ready` works, `begin`/`done` accepted alongside `start`/`complete`) plus updating `AGENTS.md`'s Work Protocol section to F#'s real `--id`/`--occurred-at`-flag syntax for the commands that differ from Node's positional/implicit-clock convention -- see AGENTS.md's "F# CLI" section, including the real-timestamp requirement a sandbox test surfaced (a transition's `--occurred-at` must not predate its execution's real-wall-clock `startedAt`, or `validate` reports a spurious chronological-order finding). `starter/greenfield/ros` (what `npx ros-bootstrap init` gives other projects) still runs Node; redirecting it is a separate, larger, not-yet-made decision needing its own distribution mechanism (no source checkout to build from in a consumer repo). Phase A's backlog-layer and live-work-layer scope is complete: all four backlog-only commands (`ready`/`block`/`abandon`, `add`/`captureWork`, `update`/`findOrCreateQueueEntry`, `attachFileUnlocked`) and all four live-work transitions (`begin`/`resume`/`block`/`complete`, including real telemetry execution creation via `startExecution` and finalization via a new `FileTelemetryFinalizationRepository`) are real effects under their respective recovery journals. Phase A has now opened MIG-08 too: `EV-ROS-2026-A044`'s inventory of `tools/ros_telemetry.mjs` explicitly declined to recommend a first slice, only that one was needed; increment 1 chose the smallest one — `telemetry adapters` (a static catalog dump) and `telemetry show` (a real, lock-free read of `.ros/telemetry/executions/*.json`) — with zero new write-path, lock, identity, or adapter complexity. Increment 2 closed a real gap increments 6-8 had left open, honestly documented at the time: `recordTelemetryLifecycle`'s within-execution "blocked"/"resumed" event bookkeeping, so `time.blocked_ms` now computes a real nonzero value at finalization instead of always zero. Increment 3 ports `telemetry summary`'s real aggregation (`summarizeTelemetry`/`timingSummary`) via a new pure `Ros.Domain.Telemetry.Summary` module. Increment 4 ships `telemetry finalize [TARGET]`, the first write-path telemetry-producer command: target resolution ports production's `resolveExecution` exactly (including its real quirk that a non-`EXE-`-prefixed target matches by workItemId regardless of status), then reuses the already-tested `FileTelemetryFinalizationRepository.finalizeOne` mutation unchanged, reproducing production's real pre-lock "already finalized" fast path; `--input` (real adapter ingestion) remains deliberately unported and rejected outright. Increment 5 ships `telemetry record [TARGET] --metric ID --value VALUE`, the second write-path telemetry-producer command: target resolution generalizes increment 4's resolver with production's own `activeOnly` option (a target whose only match is not currently active is rejected, including a race re-checked immediately after the lock), the metric id/value are validated inside the lock at the same point production's `normalizeMetric` does, and a content-addressed `measurementId` deduplicates an identical repeated call while the capability upsert still happens unconditionally, matching production's own `addMetric` exactly. Increment 6 ships `telemetry ingest [TARGET] --input FILE`, the third write-path telemetry-producer command, restricted to the generic adapter: real snapshot dedup, the declared-unavailable guard, identity/classification/scope/links/quality-signal merging, general metric normalization, `(metricId, providerField)`-keyed capability upsert (the `Capability` domain type gained an optional `ProviderField`), raw-payload redaction/truncation plus the four-branch byte-budget retention policy, and provenance-source dedup; every other real adapter name is rejected outright. Increment 7 ships `telemetry classify`, a thin wrapper over increment 6's generic-ingest path: a synthetic ingest whose only content is a constructed `classification` object and an explicit `raw: {}`, using a real-clock `` `classification-${Date.now()}` `` snapshotId (not content-addressed, so never deduplicated across repeated calls). Increment 8 ships `telemetry start WORKITEMID`, the fourth write-path telemetry-producer command and the first with no state transition to plan: it recovers an unambiguous detached candidate, rejects ambiguity with production's exact `; rerun with --execution-id one of: ...` message, or creates a new execution (`CreateExecutionRequest` gained a real, previously-unported `ClassificationRationale` field), linking it via a narrow direct context read-modify-write rather than `FileWorkContextRepository.applyContextPlan`; `--execution-id` and the eleven identity-override flags production's own CLI exposes are deliberately excluded and loudly rejected (exit 2), their own future slice. Increment 9 ships `adapter call --store FILE --request FILE`, the external work-system adapter conformance test (`DF-ROS-2026-A007`): a pure `Ros.Domain.Work.AdapterContract.decide` mirrors production's `callFileAdapter`/`validateAdapterRequest` branch order exactly (protocol/operation checks before the store file is ever touched, a cached `requestId` replay, repository authorization, `simulateOutcome: "unknown"` fault injection, then each operation's own forbidden/not-found/conflict checks), with `Ros.Infrastructure.Work.FileAdapterRepository.call` performing the actual JSON read/mutate/write -- a `transitionWorkItem` success mutates only `state`/`updatedBy` on the existing work-item node, a `publishRepositoryEvent` success dedups by `eventId`. Increment 10 ships `adapter publish --target FILE`, the paired event-republish command: appends every `.ros/events/events.jsonl` event not already present (by `eventId`) in the caller-named `--target`, then refreshes `.ros/publications.json`'s receipt for every source event -- even an already-published one -- with a fresh `publishedAt` on every call, a real quirk verified against Node before any test existed; creates the target's parent directory unconditionally, even with zero events. `resume`'s `parentExecutionId` correction (`FileTelemetryQueryRepository.readLatestExecutionId`, new; `CreateExecutionRequest.ParentExecutionId`, new, threaded into the pre-existing `Identity.discover`/`IdentityInputs.ParentExecutionId`) fixes production's own dead-code defect rather than reproducing it -- confirmed via manual smoke-testing against real, unpatched Node, whose created records still always carry `parentExecutionId: null`, documented explicitly (not silently) by a dedicated differential test running both sides side by side. Increment 11 ships `telemetry ingest --adapter openai-codex`: `adaptOpenAICodex` maps a Codex `exec --json` event stream to normalized token/context metrics, declaring a fixed capability for all six usage fields per completed entry regardless of presence but a metric only when present, resolving `identity.model` from the last record (searched in reverse) carrying one, and reproducing production's own quirk that `context.window_size` gets a metric but no adapter-level capability (the shared metric pipeline upserts one regardless). This port confirmed the generic-ingest mutation pipeline is fully adapter-agnostic: a new adapter needs only its own adaptation function plus one dispatch arm. Increment 12 ships the three hook adapters via `adaptHook`: `hook_event_name` (falling back to `hookEventName`/`event`) is matched, case-insensitively, against five independent (non-exclusive) precompiled patterns -- PostToolUse/AfterTool/ToolResult, SubagentStart, PostCompact, PermissionRequest, PermissionDenied -- each contributing its own metric(s) only when it actually matches, with `tool.calls` and a `toolCategoryMetric`-bucketed metric (11 categories by tool-name substring, falling back to `tool.external_service_calls`) always paired on a tool-use match, plus a conditional `tool.failures` when the payload's `error`/`tool_response.error` is truthy or `success` is explicitly `false`. Every capability is derived 1:1 from the metrics that actually fired (no "recognized but absent" declarations, unlike `openai-codex`'s fixed six-field pattern), and exactly one `runtime.{hookName}` event is emitted per call regardless of how many metrics fired. Increment 13 ships `telemetry ingest --adapter anthropic-claude-statusline` via `adaptClaudeStatusline`: unlike every prior adapter, its input is a single snapshot object, not an event stream, so there is no per-record iteration at all. Three top-level fields (`context.window_size`, `context.utilization` -- computed as `used_percentage / 100` -- and `cost.session_cumulative`) and four `current_usage` token fields (input/output/cache-write/cache-read) each always get a capability declaration, present or not, mirroring `openai-codex`'s fixed-declaration style rather than `adaptHook`'s derived-from-fired style; `cost.session_cumulative` is the one field whose present-value capability status becomes `"estimated"` rather than `"supported-observed"`, and whose metric alone carries a `currency: "USD"` extra and a `confidence` of `"medium"` (every other present metric's `confidence` is JSON `null`, always present as a key) -- a real quirk since a statusline snapshot cannot directly observe session cost, only estimate it. This adapter emits no events of its own at all (the shared pipeline's own `telemetry.snapshot.ingested` bookkeeping event is the only one recorded), and `collectedAt` is never overridden by an input timestamp field, unlike `adaptHook`. Increment 14 ships the OTel adapter family (`anthropic-claude-otel`/`google-gemini-otel`/`github-copilot-otel`/`otel-json`) via one shared `adaptOtel` -- the last real adapter mapping, and the first with no fixed field schema at all: every field is looked up across a per-record set of nested candidates, and `identity.provider`/`runtime`/`model`/`sessionId` can be overwritten record-by-record across the whole stream, last one wins. This closed MIG-08's telemetry-ingest adapter inventory entirely: every name in `Ros.Domain.Telemetry.TelemetryAdapters.all` now dispatches to a real F# effect. Increment 15 closes the one remaining piece of MIG-08's own scope: `telemetry start`'s own `--execution-id` and its 11 identity-override flags, both rejected outright until now. `--execution-id` needed no new decision logic -- `ExecutionLinkRecovery.decide`'s pure `RequestedExecutionId` handling was simply threaded through from the CLI for the first time -- and the 11 identity flags reuse `Identity.discover`'s already-complete override handling; `CreateExecutionRequest` gained `ExecutionId`/`IdentityOverrides` fields and `createExecution` now accepts an explicit execution id and merges the full override set over environment-discovered identity. `EV-ROS-2026-A043`'s inventory has been re-run twice: `EV-ROS-2026-A045` found 13 of 26 rows at that collection point (up from 3); `EV-ROS-2026-A046` re-ran it again after every MIG-08 increment above shipped, finding 21 of 26 (the `parentExecutionId` correction, the `openai-codex` mapping, the hook adapters, the statusline adapter, and the OTel adapter family each refined an already-flipped row rather than closing an additional one, since that inventory treats `telemetry ingest ...` as a single row regardless of adapter; `telemetry summary`/`finalize`/`record`/`ingest`/`classify`/`start` and both `adapter` commands each flipped their own previously-unflipped row). The acceptance criterion (every row) is still not met, but every remaining gap (4 pure read-only views -- `work`/`work list`, `work show`, `work context`, `status` -- plus `validate`'s own single-command unification) is outside MIG-07/MIG-08's own scope; MIG-08 itself is fully real |
| Legacy removal | none; Python validator remains an oracle; layout generator is only a deprecation candidate |
| Distribution | `DF-ROS-2026-A028` Phase B closed: `DF-ROS-2026-A029` accepts self-contained single-file binaries (`linux-x64`/`linux-arm64`/`osx-x64`/`osx-arm64`/`win-x64`) fetched from GitHub Releases and cached on first use, exposed via the npm package's own `ros-fs` bin entry (`bin/ros-fs.mjs`/`lib/ros-fs-launcher.mjs`) and, per `DF-ROS-2026-A031`, via an equivalent additive `ros-fs` scaffolded into every `ros-bootstrap init` project (`starter/greenfield/ros-fs`/`tools/ros_fs_launcher.mjs`, reading its target version from that project's own `ros.json`). `.github/workflows/publish.yml` builds and releases the binaries on a stable version bump only. Both `ros-fs` entrypoints are additive: `./ros`'s own dispatch remains Node in `starter/greenfield/ros` (deliberately -- see the Production command switch row) and F# in this repository only (`DF-ROS-2026-A030`) |
| Verification | complete gate passes: 113 Node, 7 Python, 375 F#, and 185 differential/smoke tests (680 total); zero-warning build; a direct four-tier compliance sweep (project references, per-file opens, no invented Tier 3 decisions) found no violations; the full gate and this repository's own SDE work protocol for these increments both ran through the newly-redirected `./ros` itself as the final integration proof |
| Research | experiment A020 and hypotheses A021–A026 preregistered; `EV-ROS-2026-A047` re-runs the command-surface parity inventory a third time (superseding `EV-ROS-2026-A046`, which itself superseded `EV-ROS-2026-A043`/`A045`), finding all 26 rows at full parity for the first time -- closing `DF-ROS-2026-A028` Phase A. `EV-ROS-2026-A048` then gathers Phase B's consumer distribution evidence (shape comparison, cross-platform buildability, package-size, offline/integrity/rollback), and `DF-ROS-2026-A029` accepts self-contained single-file binaries via GitHub Releases as that shape, closing Phase B. Phase C (the `./ros` switch decision itself) remains open |

T6 closed WI-0011 and finalized `EXE-20260907T203141590Z-54f547f8`. The final
push state is recorded in the handoff report and journal after the closeout
commit; production authority remains unchanged.

## Classification summary

- **To F# core/commands:** artifact rules/projection and read-only Git status now
  shadow-owned; later work, execution, telemetry, evidence, configured stable
  policy, initialization/upgrade, and release-plan decisions.
- **Retained thin adapters:** launchers, npm acquisition, HTTP, TypeScript UI,
  provider mappings, hub-to-spoke process boundary, package task glue.
- **Retained platform declarations:** GitHub YAML, profile manifests, schemas,
  metric catalog, UI/compiler configuration, SDE inputs.
- **Deprecated only after proof:** independent Python artifact CLI and legacy
  Python layout generator.
- **Deferred:** project-administration F# ownership and production distribution.
- **Blocked for missing requirements:** Time Entry semantics.

This file is updated after T3–T6; it must not be used as evidence that a planned
slice has completed.
