# ROS F# migration traceability

This table links current behavior to its authority, planned/new typed owner, and
verification. A planned owner is not authoritative until its slice and switch
decision are accepted.

| Responsibility | Current requirement/authority | SDE constraint | F# authority | Verification/status |
|---|---|---|---|---|
| Canonical Markdown and generated registries | `DF-ROS-2026-A001`, A002; Node/Python behavior | boundary preservation; explicit semantic authority | `Ros.Domain.Artifacts`, `Ros.Application.Artifacts`, `Ros.Contracts.Artifacts` | MIG-03/04 complete; Node/F# byte differential and repeatability pass; Node remains authority |
| Artifact ID/kind/status/confidence/reference validation | schemas, `tools/ros_cli.mjs`, `tools/ros_cli.py` | pure decision; explicit codecs; negative proof | `Ros.Domain.Artifacts` + `Ros.Infrastructure.Artifacts.FrontMatter` | valid/invalid frozen fixtures, malformed front matter, reference/status/ID rejection, and current-repo smoke pass |
| Live-work transition decision and plan | `DF-ROS-2026-A006`; `TRANSITIONS`, completion guards, item/event projection, and telemetry call order in work kernel | legal transitions/obligations explicit; effects planned before execution; rejection proof | `Ros.Domain.Work`, `Ros.Application.Work`, `Ros.Contracts.Work` | exhaustive matrix and guards pass; all five legal Node item/event projections match; ordered multi-item begin and later rejection match; no state effects moved |
| Whole-context transition planning | A006; production `transitionUnlocked` item loop and final event/context transaction | validate complete plan before executing effects; preserve item/event ordering and first-begin baseline | `WorkContextPlanning`, `WorkContextPlanContract`, shadow `work context-plan` | typed empty/invalid/missing/later-transition rejection tests; Node differential matches context order, request event order, new-item append, metadata, and no context write on rejection; production pre-commit telemetry side effect remains open |
| Completion evidence path observation | A006; production `fs.existsSync(path.resolve(root,path))` behavior | distinguish missing from unavailable; inspect only after semantic guards | `WorkEvidenceRepository`, `VerifiedWorkPlanOutcome`, `FileEvidenceRepository` | file, directory, absolute, and missing compatibility differential passes; injected unavailable issue remains distinct; containment policy remains open |
| Batch completion evidence composition | A006; production multi-item completion loop | reject entire semantic context before I/O; preserve missing/unavailable and request order | `WorkOperations.planVerifiedContext`, `VerifiedWorkContextPlanOutcome` | typed tests prove zero evidence calls on semantic rejection/non-completion and one ordered command-level observation on accepted completion; multi-item present/missing differential matches production |
| Telemetry execution-ID result-feedback | A006/A010; production `recoverOrStartExecution`, resume's bulk active-link loop, and `finalizeWorkExecutions` | freeze the final item/event projection only when no new execution record is required; never guess a new ID; preserve begin's single-candidate ambiguity guard versus resume's non-rejecting bulk link | `Ros.Domain.Work.TelemetryResolution`, `TelemetryPlanResolution`, `Ros.Application.Work.TelemetryStateRepository`, `Ros.Infrastructure.Work.FileTelemetryStateRepository` | typed tests cover begin recover/ambiguous/reject, resume bulk-link with and without prior linkage, finalize of linked and orphaned candidates, block/no-op, and the file repository's real-record filtering/ordering and missing-directory case; Node differential reads the same real `.ros/telemetry/executions` records production wrote (no synthetic candidates) and matches begin recovery, begin ambiguity rejection, resume multi-candidate linking, and completion recover-then-finalize and finalize-only paths |
| Backlog triage and promotion semantics | `DF-ROS-2026-A006`, A008; `BACKLOG_TRANSITIONS`, `backlogTransitionUnlocked`, `startWork` | legal transitions and promotion authority explicit; preflight before effects | `BacklogTransition`, `BacklogPromotion`, `BacklogContract` | exhaustive typed matrix and all 16 Node action comparisons pass; `start` is an explicit promotion that does not mutate queue state; batch rejection/direct external ID compatibility pass; Node remains writer |
| Backlog queue/projection persistence | A006/A008; production Node backlog kernel | whole read/modify/write capability; explicit ordered write set; retry preconditions and divergence outcome | shared Node/F# `backlog-state` journal contract; `Ros.Infrastructure.Work.BacklogStateTransaction` is typed implementation | partial replay works in both runtime directions; divergence rejects before writes; eight concurrent production captures retain every entry; Node remains command authority |
| Live transition event/context persistence | A006; production Node transition kernel | explicit write set, retry preconditions, conflict/unknown outcome, lock capability | shared Node/F# `work-state` journal contract; `Ros.Infrastructure.Work.WorkStateTransaction` is the typed implementation | MIG-05 passes normal production commit, Node recovery of a partial F#-compatible record, F# recovery of a Node-shaped record, divergence preflight, reordered/incomplete set, existing journal, and corrupt-journal tests; Node remains command authority |
| Git status/provenance observation | A006; work/telemetry kernels; porcelain-v1 behavior | effects isolated; unavailable distinct from clean; explicit boundary contract | `Ros.Domain.Git`, `Ros.Application.Git`, `Ros.Infrastructure.Git`, `Ros.Contracts.Git`; compatible installed adapter `tools/ros_git.mjs` | MIG-06 complete; exact Node/F# clean/changed/unavailable differential, rename/copy, malformed output, missing tool, and non-repository proofs pass |
| Observed/meaningful Git path composition for context planning | A006; production `gitPaths`/`meaningfulPaths`/`workConfig` in `tools/ros_cli.mjs` | real observation by default, gated on the same action condition production gates on; config-driven glob include/ignore, not a hardcoded set; unresolvable base ref is silent, a failing resolvable one is not | `Ros.Domain.Work.PathFilter`, `Ros.Domain.Git.GitBaseComparisonOutcome`, `Ros.Infrastructure.Work.FileWorkConfigRepository`, `ProcessGitRepository.createBaseComparison` | typed glob-match parity cases (including `?`/`[...]` characters Node's manual escape leaves literal), default/custom config field-by-field fallback, and base-comparison unit tests; Node differential matches real first-begin baseline capture, ROS-housekeeping-path exclusion from completion paths, custom configured patterns, a resolvable `ROS_BASE_REF` committed range, and a silently skipped unresolvable one |
| Completion evidence and Git attribution | A006; `ros.json`; work kernel | effects isolated; unavailable distinct from clean | F# Git contract plus compatible Node effect adapter; future Work handler consumes the F# port directly after distribution | work records rename destination; unavailable completion rejects before telemetry finalization and validation emits a structured `.git` finding; greenfield pre-Git compatibility is explicit |
| Backlog-queue validation (`queueFindings`) | A006/A008; production `queueFindings` in `tools/ros_cli.mjs`, another contributor to the `validate` findings pipeline | validate the raw persisted rows, not an already-parsed enum, so an unrecognized value is a finding, never a parse throw; duplicate ids flagged on the second occurrence, not the first | `Ros.Domain.Work.BacklogQueueValidation`, `Ros.Contracts.Work.BacklogQueueValidationContract`, `Ros.Infrastructure.Work.FileBacklogQueueRepository`, shadow `work backlog-validate` | typed duplicate/invalid-id/invalid-status/invalid-priority decision tests plus a repository unit test proving the same empty-file default as production's `loadQueue`; Node differential (filtering production `validate`'s findings to `path === ".ros/work/queue.json"`) matches a well-formed queue, a duplicate id, an invalid id, an invalid status, an invalid priority, and an absent queue file |
| Backlog-only transition effect (`backlogTransition`) — DF-ROS-2026-A028 Phase A, increment 1 | A006/A008; production `backlogTransition`/`backlogTransitionUnlocked`/`saveQueueUnlocked` in `tools/ros_cli.mjs` | a real write, not a diagnostic; preserve every unmodeled field/item verbatim (JSON surgery, not a typed round trip); same file lock, recovery journal, and error messages as production; excludes `start` (a separate, telemetry-entangled production effect) | `Ros.Domain.Work.BacklogTransition` (decision, already existed), `Ros.Domain.Work.QueuePresentation` (markdown projection), `Ros.Infrastructure.Work.FileBacklogQueueRepository.applyStateChange`, `Ros.Contracts.Work.BacklogTransitionEffectContract`, shadow-turned-real `work backlog-transition` | typed tests prove field/item preservation, `Clear` removing (not nulling) a reason, and merged-markdown correctness for an untouched live item; a real Node differential (structural `queue.json` equality ignoring the clock-derived `updatedAt`, byte-identical `queue.md`, and exact-message rejection with no file write) covers block/ready/abandon, an illegal transition, a reasonless block, and an unknown id — including a title with an apostrophe and embedded quotes, proving `JavaScriptEncoder.UnsafeRelaxedJsonEscaping` matches production's own `JSON.stringify` escaping |
| Work capture effect (`add`/`captureWork`) — DF-ROS-2026-A028 Phase A, increment 2 | A006/A008; production `captureWorkUnlocked`/`nextQueueId`/`saveQueueUnlocked` in `tools/ros_cli.mjs` | a real write, excluding `--file` attachment (separate effect); explicit-id checked against both the queue and the live context, in that order; a generated id checked only against the queue (a real production asymmetry, reproduced rather than corrected); the same default document as `loadQueue` when `queue.json` is absent | `Ros.Domain.Work.WorkCapture` (new pure decision), `Ros.Infrastructure.Work.FileBacklogQueueRepository.captureItem`, `Ros.Infrastructure.Work.FileWorkConfigRepository.readRepositoryId` (new), reused `Ros.Domain.Work.QueuePresentation` and `Ros.Contracts.Work.BacklogTransitionEffectContract`, shadow-turned-real `work capture` | typed tests cover title trimming/emptiness, priority validation, explicit-id validation order, generated-id collision-skipping and its queue-only scope, and description/default fallbacks; infrastructure tests prove field/item preservation on an existing queue and correct document synthesis on an absent one; a real Node differential covers an auto-generated id (with an apostrophe-and-quotes title), a completely fresh `queue.json`, an explicit id that never advances `nextSeq`, and three rejections (empty title, invalid priority, duplicate explicit id) — all structurally/byte-identical to production modulo the clock-derived timestamps |
| Work update effect (`update`/`findOrCreateQueueEntry`) — DF-ROS-2026-A028 Phase A, increment 3 | A006/A008; production `updateWorkUnlocked`/`findOrCreateQueueEntry`/`saveQueueUnlocked` in `tools/ros_cli.mjs` | a real write, excluding `--file` attachment; only fields explicitly provided change (`Keep`/`Set`, never inferred from a prior value); an id absent from the queue but present in the live context is upserted with production's exact minimal-record defaults before the requested changes apply; `--tag`'s presence, not its value, decides whether tags change at all | `Ros.Domain.Work.WorkUpdate` (new pure decision: `WorkTitleChange`/`WorkDescriptionChange`/`WorkTagsChange`/`WorkPriorityChange`), `Ros.Infrastructure.Work.FileBacklogQueueRepository.applyUpdate` (shares `loadOrCreateQueueNode`/`commitQueue` with `captureItem`, extracted without behavior change), shadow-turned-real `work update` | typed tests cover in-place update vs. context-only upsert, the id/context/queue rejection order, every field's Keep-when-omitted default, description's trim-to-clear behavior, tag replacement including an explicit empty list, and title-before-priority validation order; infrastructure tests prove field/item preservation, JSON-null (not string) clearing, minimal-default upsert shape, and default-document synthesis on a missing file; a real Node differential covers title/description/priority together, description-clear, tags left untouched when `--tag` is never passed, a context-only upsert, and two rejections (unknown id, empty title) — all structurally/byte-identical to production modulo the clock-derived timestamps |
| Work attachment effect (`attachFileUnlocked`/`sanitizeFileComponent`) — DF-ROS-2026-A028 Phase A, increment 4 (backlog-only effects complete) | A006/A008; production `attachFileUnlocked`/`sanitizeFileComponent`/`findOrCreateQueueEntry` in `tools/ros_cli.mjs` | the first real effect touching file bytes, not just JSON/text; the attachment binary write is a plain, non-transactional write before the queue commit, reproducing production's own non-atomicity rather than improving on it; next sequence is one past the highest existing sequence (missing/malformed `seq` counts as zero), not the count; `N` `--file` flags commit as `N` separate lock cycles, not one batch; `contentType` is always absent, matching the real CLI path | `Ros.Domain.Work.WorkAttachment` (new pure decision + `sanitizeFileComponent` port), `Ros.Infrastructure.Work.FileBacklogQueueRepository.applyAttachment`/`readAttachmentSequences`, a `findOrAppendItem` helper extracted from `applyUpdate` (shared by all three item-touching effects, behavior unchanged), shadow-turned-real `work attach` (`attachOneFile` looped once per `--file`) | typed tests cover the sanitizer's basename/trim/collapse/fallback rules (including the corrected `"???" -> "_"` case, not `"file"`), sequence computation from a real gap-and-duplicate set, the id/context/queue rejection order, and record-shape defaults; infrastructure tests prove real byte writes, attachment-array append with prior records preserved, and context-only upsert; a real Node differential covers a named attachment (with an apostrophe-and-quotes item title), a defaulted-to-basename name, a context-only upsert, and two rejections (unknown id, invalid id shape) — including a byte-for-byte comparison of the written attachment file itself, not just `queue.json` |
| Work start effect (`startWork`/`transitionUnlocked`'s `begin` action, and `startExecution`) — DF-ROS-2026-A028 Phase A, increment 5 (first live-work/telemetry-creating effect) | A006/A008/A010; production `startWork`/`transitionUnlocked`/`commitWorkState`/`startExecution` in `tools/ros_cli.mjs`/`tools/ros_telemetry.mjs` | a real write to `.ros/context/current.json` and `.ros/events/events.jsonl` (JSON-node surgery preserving unmodeled item fields, e.g. a research item's `conclusion`) plus, when no recoverable execution exists, a real `.ros/telemetry/executions/EXE-*.json` write; identity discovered purely from whitelisted environment variables, never explicit CLI overrides; event `eventId` hashed over exact production key-insertion order (an absent `reason` dropped from the hash input, not hashed as null), distinct from `measurementId`'s sorted-key digest; a capability `initialCapabilities` seeded and never merged into keeps production's exact 5-key shape, no `lastAssessedAt`/`recordedAt`/`history`/`historyOmitted` fields at all; excludes `resume`/`block`/`complete`, explicit identity/execution-id overrides, and `recordTelemetryLifecycle` | `Ros.Domain.Telemetry.Identity`, `Ros.Domain.Telemetry.Capability`/`WorkClassification` (new pure decisions), `Ros.Infrastructure.Work.FileTelemetryExecutionRepository.createExecution`, `Ros.Infrastructure.Work.FileWorkContextRepository.applyContextPlan`, `Ros.Infrastructure.Work.FileMetricRegistryRepository`, `Ros.Infrastructure.Git.ProcessGitRepository.readBranchAndCommit` (new), `Ros.Infrastructure.Json.CanonicalJson` (new, shared event/measurement hashing primitive), composed with the already-existing `WorkContextPlanning`/`TelemetryPlanResolution` decision layer (MIG-07-WORK-CONTEXT/MIG-07-TELEMETRY-RESOLUTION), shadow-turned-real `work start` | typed tests cover the identity whitelist cascade (explicit override precedence, each provider's environment variable, independent session/conversation/run-id fallback chains), capability seeding per collection type, capability upsert history/cap/omission bookkeeping, and default classification; infrastructure tests prove unmodeled-field preservation, conditional `telemetryExecutionIds` omission, event-log dedup, and telemetry-disabled no-op; a real Node differential covers a brand-new item (full execution-record structural equality modulo clock/random fields), a ready-backlog-item promotion (including byte-identical `queue.json`), and three rejections (captured, abandoned, already-active) with production's exact messages |
| Work resume effect (`transition(root, "resume", ...)`) — DF-ROS-2026-A028 Phase A, increment 6 | A006/A008/A010; production `transitionUnlocked`'s `resume` branch in `tools/ros_cli.mjs` | a real write reusing increment 5's effect infrastructure unchanged (no new Domain/Infrastructure code): never creates a new context item (an absent id is rejected, not upserted); never observes Git (production's own `gitPaths` gate excludes `resume`); `EnsureActiveExecution` links every currently-active candidate when one exists (`linkAllActive`) or creates a new execution when none is active; excludes `recordTelemetryLifecycle`'s "resumed" bookkeeping and production's `parentExecutionId` linkage on the new-execution path (no CLI exposes either) | a new `resolveContextTelemetryWithCreation` shared helper factored out of `runWorkStart` (behavior unchanged, `work start`'s own differential still passes), reused verbatim by `runWorkResume`; no other new code | a real Node differential covers resuming a blocked item with an active execution already linked (full context/event structural equality), resuming a item with no active execution at all (a state no current CLI command can produce, seeded directly as fixture state — proves the create-new-execution path), and two rejections (id not in context, illegal transition from active) with production's exact messages |
| Work block effect (`blockWork`) — DF-ROS-2026-A028 Phase A, increment 7 | A006/A008/A010; production `blockWork`/`backlogTransitionUnlocked`/`transitionUnlocked`'s `block` branches in `tools/ros_cli.mjs` | a real write reproducing production's own id-splitting between a not-yet-started backlog item and an already-live context item, applying the matching real effect to each under ONE held `work-protocol` lock, returning a single mixed-shape array; `--reason` is checked by the existing decision layer, not eagerly at the CLI, so an item in the wrong state gets the illegal-transition rejection first, matching production's own check order exactly; fixed a real bug this slice's own differential caught: `applyItem` never wrote `blockReason` onto a live-context item at all (now written whenever `Item.BlockReason` is `Some`, for every action, matching production's own never-cleared persistence) | `applyBacklogTransition`, a helper extracted from `runBacklogTransitionEffect` (behavior unchanged) and reused by the backlog branch of `runWorkBlock`; the context branch reuses increment 5/6's effect infrastructure verbatim; `FileWorkContextRepository.applyItem` fixed to write `blockReason` | a real Node differential covers blocking a not-yet-started backlog item, blocking an already-live item, splitting one call's ids across both branches at once (including byte-identical `queue.json` and structural context equality), `--reason` required only once an item is legally blockable (proving the check-order fix), and an id in neither the queue nor the live context |
| Work complete effect (`transitionUnlocked`'s `complete` action, and `finalizeWorkExecutions`) — DF-ROS-2026-A028 Phase A, increment 8 (closes every live-work transition) | A006/A008/A010; production `transitionUnlocked`'s `complete` branch and `finalizeExecution`/`finalizeWorkExecutions`/`cleanBaselineChanges`/`blockedDuration` in `tools/ros_cli.mjs`/`tools/ros_telemetry.mjs` | reuses increment 5/6's context-effect infrastructure, adding real evidence-path verification (`WorkOperations.planVerifiedContext`/`FileEvidenceRepository`, checked only after the frozen decision layer accepts the required evidence *types*) and per-type required evidence read from `ros.json`'s `workProtocol.completionEvidence` (`FileWorkConfigRepository.readCompletionEvidence`, matching production's per-type-then-`default` fallback, e.g. `research` requiring `research-record` instead of `implementation`/`tests`); Git is always observed (production's own `gitPaths` gate includes `action === "complete"` unconditionally); telemetry finalization is called directly and unconditionally per completing id (`TelemetryPlanResolution` discards the `FinalizeExecutions` intent signal, so this is not threaded through the shared resolution pipeline, matching production's own unconditional call in the same position — before the context/events commit); a research item's `--conclusion` (defaulting to `"inconclusive"`) is written via a new `applyContextPlanWithConclusions`; excludes `options.input`/adapter-ingestion (unreachable from any CLI path) and explicit `--identity-*`/`--execution-id` overrides | `Ros.Domain.Telemetry.ChangeSummary` (new: `ChangeClassification`, `ChangeSummaryParser`, `BlockedDuration` -- reduced to the fields `finalizeExecution` actually reads, excluding `filesByType`/per-file `paths`), `Ros.Infrastructure.Work.FileTelemetryFinalizationRepository` (new: `finalizeWorkExecutions`, a real per-execution-locked mutation of `.ros/telemetry/executions/EXE-*.json` computing a real `git diff`-derived clean-baseline change summary via three new `ProcessGitRepository` reads — name-status, numstat, and untracked files — or one of production's exact three unavailable reasons), `FileWorkContextRepository.applyContextPlanWithConclusions` (new, `applyItem` extended to also persist `completedAt` and a research item's `conclusion`), `FileWorkConfigRepository.readCompletionEvidence` (new), `FileTelemetryExecutionRepository.derivedMetricNode`/`observeGitBaseline`/`GitBaseline` generalized from `createExecution`-only to shared finalization use, shadow-turned-real `work complete` | typed tests cover `ChangeSummaryParser`'s name-status/numstat/untracked parsing and aggregation, test/documentation classification, and `BlockedDuration`'s closed/open/multi-interval computation; a real Node differential covers a clean-baseline completion with real untracked additions and a tracked modification (full change-summary field equality, cross-checked git-derived counts), a preexisting-dirty-worktree-at-start completion (byte-identical unavailable change summary and capability reasons), missing-required-evidence-type and nonexistent-evidence-path rejections with production's exact messages, and a research item's default-versus-explicit `conclusion` |
| Telemetry read commands (`telemetry adapters`, `telemetry show`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 9 (first telemetry-producer commands) | A010; production `TELEMETRY_ADAPTERS` constant and `showTelemetry` in `tools/ros_telemetry.mjs` | the deliberately smallest first MIG-08 slice `EV-ROS-2026-A044` called for: zero new write-path, lock, identity, or adapter complexity. `telemetry adapters` prints a hardcoded catalog verbatim. `telemetry show [TARGET]` reads `.ros/telemetry/executions/*.json` with no lock/write; `TARGET` is positional (matching production's own `telemetryTarget(args)`), not a `--` flag like every other ported command; no target lists everything, an `EXE-`-prefixed target resolves exactly one record or production's exact rejection message, any other target filters by work-item id with an empty (not rejected) result for no match | `Ros.Domain.Telemetry.TelemetryAdapters` (new, a direct port of `TELEMETRY_ADAPTERS`), `Ros.Infrastructure.Work.FileTelemetryQueryRepository` (new: `readAll`/`readByWorkItemId`/`readByExecutionId`), shadow-turned-real `telemetry adapters`/`telemetry show` | typed tests cover empty-directory/filename-ordering/no-match/unknown-id/known-id read paths and the adapter catalog's exact order; a real Node differential (importing `showTelemetry`/`TELEMETRY_ADAPTERS` directly from `tools/ros_telemetry.mjs`) covers the adapter catalog, listing every record across two real `work start`-created executions, an empty executions directory, work-item filtering (including an unmatched id's empty result), and both an `EXE-`-prefixed resolution and its unknown-id rejection message |
| Telemetry lifecycle bookkeeping (`recordTelemetryLifecycle`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 10 (closes a real gap left open by increments 6-8) | A010; production `recordTelemetryLifecycle` in `tools/ros_telemetry.mjs`, called from `transitionUnlocked`'s `resume`/`block` branches | appends a `work.resumed`/`work.blocked` event -- deduped by a `TEVT-` id built from a full 4-key digest (including a nested 3-key `source`), the first `TEVT-`/`MEAS-` id in this migration built from more than a 2-14-key flat digest -- plus its paired `agent.resumes`/`agent.interruptions` metric, to every currently-active execution linked to a work item; `work resume`/`work block` call this BEFORE telemetry resolution runs, matching production's exact ordering (a brand-new execution created by the same transition never receives a lifecycle event for the transition that created it); closes the gap that left `time.blocked_ms` always zero at finalization | `Ros.Infrastructure.Work.FileTelemetryFinalizationRepository.recordLifecycle` (new, alongside `finalizeWorkExecutions` in the same module), reusing `FileTelemetryExecutionRepository.derivedMetricNode`/`sourceNode` and `Ros.Domain.Telemetry.Capability.upsert`; `runWorkResume`/`runWorkBlock` (`src/Ros.Cli/Program.fs`) updated to call it before `resolveContextTelemetryWithCreation` | typed tests cover event/metric appending on an active execution (both lifecycle types), a no-op on an already-finalized execution, a no-op with no linked execution at all, and eventId-based dedup on a repeated call; a real Node differential runs a full `work block` → `work resume` → `work complete` cycle, comparing the linked execution's own lifecycle events/metrics/capability status structurally and asserting an exact `time.blocked_ms` value on the F# side (deterministic from synthetic `--occurred-at` timestamps) against a merely-positive value on the Node side (real wall-clock timing) |
| Telemetry summary aggregation (`summarizeTelemetry`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 11 | A010; production `summarizeTelemetry`/`timingSummary` in `tools/ros_telemetry.mjs` | the largest read-only telemetry command: four real aggregation strategies (`sum` with a fixed note for `time.wall_ms`, `maximum`, `latest-per-session` -- a real per-session dedup, not a plain sum -- and `none`, falling back to a plain "latest wins" default) plus an interval-sweep `timingSummary` (calendar span vs. total wall time vs. overlap, all three nulled whenever any execution in the set is still active); groups every `(id, unit, currency, dimensions)` tuple across matching executions in file/array encounter order (`latest-per-session`'s tie-breaking depends on it), resolving each group's aggregation from the metric registry when known else the group's own stored `aggregation` field; `TARGET` is positional like `telemetry show` but used purely as a work-item-id filter, never special-casing an `EXE-`-prefixed value the way `show` does (matching a real production quirk); one narrow documented reduction: a measurement's `dimensions` grouping key uses its own JSON text rather than production's canonically-sorted one, since every metric this migration writes always has empty `dimensions` | `Ros.Domain.Telemetry.Summary` (new: `TimingSummary`, `MetricAggregation`, `TelemetrySummary`, all pure), `Ros.Infrastructure.Work.FileTelemetryQueryRepository.readSummaryExecutions` (new, JSON extraction/grouping input), shadow-turned-real `telemetry summary`/`telemetry summarize` | typed tests cover every `TimingSummary` branch (no executions, a still-active execution, non-overlapping and overlapping finalized spans) and every `MetricAggregation` strategy including the `latest-per-session` tie-break and registry-vs-self aggregation fallback; a real Node differential covers a full real `work start`/`work complete` cycle across two work items (cross-checked deterministic git/documentation metrics, a merely-positive `time.wall_ms` on both sides), work-item filtering, an empty-executions summary, and hand-written fixture executions exercising `latest-per-session`/`none` (byte-identical full-summary equality against production, since these paths use no real-clock-derived fields at all) |
| Telemetry manual finalization (`finalizeExecution`/`resolveExecution`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 12 | A010; production `finalizeExecution`/`resolveExecution` in `tools/ros_telemetry.mjs` | the first write-path telemetry-producer command: target resolution ports `resolveExecution` exactly (`EXE-`-prefixed exact match; any other target matches by `workItemId` regardless of status; no target requires exactly one `active`/`blocked` work item, else production's exact ambiguity message; every branch takes the latest-`startedAt` match, else production's exact not-found message), then reuses the already-tested `FileTelemetryFinalizationRepository.finalizeOne` mutation unchanged -- no second finalization code path; reproduces production's real pre-lock fast path (an already-`"finalized"` resolved execution returns untouched with no lock acquired, a real race-tolerant quirk, not a bug); `--input` (real adapter ingestion in production) is deliberately not ported and rejected outright (exit 2, `"telemetry finalize --input is not yet supported by this CLI"`) rather than silently ignored, scoped together with the still-open adapter commands; `--quiet` matches production's own flag | `Ros.Infrastructure.Work.FileTelemetryFinalizationRepository.resolveFinalizeTarget`/`finalizeTarget` (new), shadow-turned-real `telemetry finalize` | typed tests cover every resolution branch (`EXE-` exact match, workItemId match taking the latest regardless of status, single/zero/multiple active-item resolution, unknown-target rejection, already-finalized untouched) plus a real Node differential covering a full `work start` + `telemetry finalize` cycle (cross-checked git/documentation metrics), workItemId-latest-regardless-of-status and `EXE-`-prefixed resolution over hand-written fixture executions, both ambiguity variants and the not-found rejection with production's exact messages, the already-finalized byte-identical-file fast path, and an F#-only assertion that `--input` is rejected with exit code 2 |
| Telemetry explicit metric recording (`recordTelemetryMetric`/`normalizeMetric`/`addMetric`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 13 | A010; production `recordTelemetryMetric`/`normalizeMetric`/`addMetric` in `tools/ros_telemetry.mjs` | the second write-path telemetry-producer command: target resolution generalizes `finalize`'s own resolver with production's `activeOnly` option (a target whose only match is not currently `"active"` -- including a race where it finalized between the pre-lock resolve and the lock being acquired -- is rejected with production's exact `"... was not found or is already finalized"` message, checked again immediately after the lock, matching production's own re-resolve); the metric id must exist in the registry and the value must be finite, both validated inside the lock after that race re-check, matching production's exact validation order and exact error messages; `--unit`/`--currency` override the registry's defaults, `--quality`/`--scope`/`--source-*` default exactly as production's CLI does, `--pricing-source`/`--pricing-version` build a pricing object only when either is given, and `--confidence` mirrors production's permissive number-or-text-or-null parsing via a `MetricConfidence` union; the content-addressed `measurementId` (a sorted-key SHA-256 digest, mirroring `derivedMetricNode`) deduplicates an identical repeated call while the capability upsert and record rewrite still happen unconditionally, matching production's own `addMetric` exactly, including the quirk that the CLI's printed `metrics.at(-1)` can show an unrelated metric on a content-duplicate call | `Ros.Infrastructure.Work.FileTelemetryFinalizationRepository.resolveExecutionTarget` (generalized from `resolveFinalizeTarget`)/`recordMetric` (new), shadow-turned-real `telemetry record` | typed tests cover every resolution branch (`EXE-` exact match, workItemId match restricted to the active sibling, single/zero/multiple active-item resolution, the not-active rejection, unknown-metric and non-finite-value rejections, content-addressed dedup, a fresh capability creation and an existing-capability-to-history upsert, `--unit`/`--currency` overrides, and both confidence variants) plus a real Node differential covering a full `work start` + `telemetry record` cycle (byte-identical metric output against production's own `recordTelemetryMetric`), workItemId-restricted-to-active and `EXE-`-prefixed resolution over hand-written fixtures, both ambiguity variants and the not-active rejection with production's exact messages, unknown-metric/non-finite-value rejections, content-addressed dedup, a real registry-seeded capability transition, and unit/currency/confidence overrides cross-checked byte-for-byte against production |
| Telemetry generic-adapter ingestion (`ingestTelemetry`/`adaptInput`/`ingestAdapted`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 14 | A010; production `ingestTelemetry`/`adaptGeneric`/`ingestAdapted` in `tools/ros_telemetry.mjs` | the third write-path telemetry-producer command, restricted to the `generic` adapter (every other real adapter name rejected outright, exit 2; a genuinely unknown name gets production's own exact message); ports `ingestAdapted` field-for-field: snapshot dedup keyed by a `snapshotId` computed from the *original, un-adapted* input (a bug this increment's own manual smoke-testing against real Node caught and fixed before any automated test existed); the declared-unavailable consistency guard; identity merge (skip-null) versus classification/scope merge (explicit-null-overwrites) kept as two distinct functions; general metric normalization honoring every field an ingested metric item itself supplies; capability upsert now keyed by `(metricId, providerField)` -- `Capability.MetricId` is now optional and `ProviderField` was added, needed for unknown-field-discovery capabilities that carry no `metricId` at all; raw-payload redaction/truncation plus the four-branch byte-budget retention policy (new `FileWorkConfigRepository` readers for the raw-telemetry config); the three derived quality metrics routed through the same metric-normalization path; and provenance-source dedup by content digest | `Ros.Domain.Telemetry.Capability` (extended: optional `MetricId`/`ProviderField`, `Capability.upsert`'s new `newLastAssessedAt` parameter), `Ros.Infrastructure.Json.CanonicalJson.stabilize`/`contentDigest` (new, general arbitrary-JSON digest), `Ros.Infrastructure.Work.FileTelemetryFinalizationRepository.ingestTarget`/`ingestAdaptedGeneric`/`adaptGeneric`/`parseIngestInput` (new), shadow-turned-real `telemetry ingest` | typed tests cover identity/metric/event merging with redaction and derived metrics, snapshot dedup, declared-capability upsert-into-history, the declared-unavailable conflict, classification/scope/links merge semantics, quality-signal dedup, every resolution/adapter rejection, all three raw-retention-policy branches (disabled, byte-limit, snapshot-count-limit), EXE-prefixed/no-target resolution, and `parseIngestInput`'s JSON/JSON-Lines/empty/malformed handling, plus a real Node differential covering the same real-effect surface byte-for-byte against production's own `ingestTelemetry`, including snapshotId/eventId digest equality independent of executionId or wall-clock time, the declared-unavailable rejection's shared snapshotId, config-driven retention behavior, and stdin (`-`) input |
| Telemetry classification (`telemetry classify` over `ingestTelemetry`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 15 | A010; production `telemetry classify` dispatch (`tools/ros_cli.mjs`) calling `ingestTelemetry` | a thin wrapper over increment 14's generic-ingest path: builds a synthetic ingest whose only real content is a constructed `classification` object (`types`/`rationale`/`evidence`/`rd`) and an explicit `raw: {}`, then delegates to `ingestTarget` unchanged, no `--adapter` override; the one distinct behavior is `classification`'s own `snapshotId` (`` `classification-${Date.now()}` ``, a real-clock millisecond timestamp, not a content digest), so a repeated call is never deduplicated the way a normal ingest snapshot would be -- each call is its own event; `--rd-context`'s file (or `-` for stdin) is read exactly like `telemetry ingest`'s own `--input` (`parseIngestInput`, reused rather than duplicated), and the missing-`--classification` check happens before that read, matching production's own check order | `Ros.Infrastructure.Work.FileTelemetryFinalizationRepository.classifyTarget` (new, thin), shadow-turned-real `telemetry classify` | typed tests cover the classification shape (types/rationale/evidence/no-rd), the empty-classification-list rejection with production's exact message, a supplied rd-context merge, non-dedup across repeated calls, and EXE-prefixed target resolution, plus a real Node differential (reproducing production's own CLI-dispatch construction, since it has no standalone exported function) covering the same classification shape byte-for-byte, `--rd-context` file merging, the empty-classification rejection, and non-dedup across two real calls |
| Telemetry manual attach (`telemetry start` over `recoverOrStartExecution`) — DF-ROS-2026-A028 Phase A / MIG-08, increment 16 | A010; production `telemetry start` dispatch (`tools/ros_cli.mjs`) calling `recoverOrStartExecution` | the first command that recovers-or-creates telemetry outside a work transition: no plan to build, since there is no state change -- rejects a work item that is missing from context or not `active`/`blocked` with production's identical message from either cause; recovers an unambiguous detached (unlinked, `active`) candidate, rejects on more than one with production's exact `; rerun with --execution-id one of: ...` message, else creates a new execution via `FileTelemetryExecutionRepository.createExecution`, bounded to a single attempt; links the resolved id into `telemetryExecutionIds` (guarded against a duplicate) and bumps `context.updatedAt`, but -- matching production's own `renderedEventLog(root, [])` -- never appends to `events.jsonl`; ported `classificationRationale` as a real, previously-unported field onto `CreateExecutionRequest`; deliberately excludes and loudly rejects (exit 2) `--execution-id` and all eleven identity-override flags production's own CLI exposes (`telemetryIdentityOptions`), scoped as a separate future slice since supporting them requires extending `createExecution` itself | `Ros.Infrastructure.Work.FileTelemetryExecutionRepository.CreateExecutionRequest` (extended: `ClassificationRationale`), `FileTelemetryFinalizationRepository.resolveOrCreateExecution`/`startTarget` (new, calling `Ros.Domain.Telemetry.ExecutionLinkRecovery.decide`/`FileTelemetryStateRepository.readCandidates` directly rather than `FileWorkContextRepository.applyContextPlan`), shadow-turned-real `telemetry start` | typed tests cover fresh creation with no candidate, detached-execution recovery, new-execution-when-only-candidate-already-linked, ambiguity rejection with production's exact message, the not-active-or-blocked rejection for both a missing and a wrong-state item, and the disabled-with-no-candidate no-op; a real Node differential covers the same seven scenarios end-to-end against the real `ros` CLI (fresh creation, detached recovery, re-creation-when-linked, ambiguity, not-active-or-blocked for both causes, disabled-with-no-candidate returning `null` with no context mutation) plus an F#-only assertion that `--execution-id` is rejected with exit code 2 |
| External work-system adapter conformance (`adapter call` over `callFileAdapter`) — DF-ROS-2026-A007, tracked as part of `DF-ROS-2026-A028` Phase A/MIG-08's remaining scope | A007; production `callFileAdapter`/`validateAdapterRequest` (`tools/ros_cli.mjs`), `docs/work-adapter-contract.md`, `schemas/work-adapter-request.schema.json`/`work-adapter-result.schema.json` | the dependency-free file-based conformance adapter for the external work-system contract, exercising `getWorkItem`/`transitionWorkItem`/`publishRepositoryEvent` against a caller-named standalone JSON store with no `work-protocol` lease; work items/events are caller-defined open-ended JSON, so only the handful of fields production itself inspects (a work item's `state`/`updatedBy`, an event's `eventId`) are typed, everything else preserved verbatim; a protocol-version mismatch or unsupported operation never touches the store file at all (matching production's own pre-load `validateAdapterRequest`); a cached `requestId` replay returns the prior result unmutated; repository authorization and `simulateOutcome: "unknown"` fault injection are checked before any operation-specific logic; every result (success, failure, or unknown) is cached under its `requestId` and the store rewritten exactly once per non-replayed call; exit codes mirror production's own CLI mapping (0/1/2) | `Ros.Domain.Work.AdapterContract` (new, pure `decide`/`firstMissingField`), `Ros.Infrastructure.Work.FileAdapterRepository` (new), shadow-turned-real `adapter call` | typed tests cover every `decide` branch (protocol mismatch, unsupported operation, cached replay, repository unauthorized, fault injection, per-operation forbidden/not-found/conflict/success) and the infra effect (missing-field rejection untouched store, protocol-mismatch untouched store, read success, transition success/persistence/idempotent replay, state conflict leaving the item untouched, event publish/dedup, invalid-event rejection, unknown-outcome leaving the item untouched, missing-store-file treated as empty); a real Node/F# differential covers the same read/transition/idempotent-retry/conflict/enforcement/publish-dedup/missing-field/missing-file scenarios byte-for-byte against production's own CLI |
| Adapter event republish (`adapter publish` over production's own republish command) — DF-ROS-2026-A007, tracked as part of `DF-ROS-2026-A028` Phase A/MIG-08's remaining scope | A007; production's `adapter publish` dispatch (`tools/ros_cli.mjs`) | reads every event from `.ros/events/events.jsonl`, appends whichever are not already present (by `eventId`) in a caller-named `--target` destination JSONL file (unrelated to `adapter call`'s own store), then refreshes `.ros/publications.json`'s receipt for every source event -- even an already-published one -- with a fresh `publishedAt` on every call, a real quirk verified against Node before any test existed; creates the target's parent directory unconditionally (even with zero events, creating the directory but no destination file); a directory-creation or append failure propagates before either the destination or receipts file is touched | `Ros.Infrastructure.Work.FileAdapterRepository.publish` (new, alongside `call`), shadow-turned-real `adapter publish` | typed tests cover fresh-destination append, eventId dedup across repeated calls, the unconditional per-call receipt refresh, the zero-event directory-creation-without-a-file quirk, and a directory-creation failure leaving neither file touched; a real Node/F# differential covers append/dedup/receipt-count byte-for-byte against production's own `work begin` + `adapter publish` cycle, the write-failure rejection, the missing-`--target` message, and the zero-event case |
| Telemetry provider-specific field mapping (`telemetry ingest --adapter openai-codex` over `adaptOpenAICodex`) — DF-ROS-2026-A028 Phase A/MIG-08, increment 19, the first real adapter beyond `generic` | A010; production `adaptOpenAICodex` (`tools/ros_telemetry.mjs`) | maps a Codex `exec --json` event stream (or a single event object) to normalized token/context metrics; `completed` entries (`type === "turn.completed"` or any `usage` object) each declare a fixed capability for all six usage fields regardless of presence, but a metric only for the ones actually present, in production's own per-entry per-field order; `context.window_size` is the one metric with deliberately no paired capability declaration in the adapter itself (though the shared metric-recording pipeline upserts one regardless, since that step is adapter-agnostic); `identity.model` resolves from the last record (searched in reverse) carrying a truthy `server_model` or `model`, even one that is not itself "completed"; confirms the generic ingest pipeline (`ingestAdaptedGeneric`) is fully adapter-agnostic, so porting a new adapter means only writing its adaptation function and one dispatch arm | `Ros.Infrastructure.Work.FileTelemetryFinalizationRepository.adaptOpenAICodex` (new), `ingestTarget`'s dispatch extended for `"openai-codex"` | typed tests cover the fixed six-field capability declaration with presence-gated metrics, the context.window_size metric-without-adapter-capability quirk, per-entry turnIndex assignment, reverse-order identity.model resolution (including a non-completed identity-bearing record), the completed-entry filter, and single-object (non-array) input wrapping; a real Node differential covers identity/capabilities/metrics/events byte-for-byte against production's own `ingestTelemetry`/`adaptOpenAICodex`, the reverse-order identity resolution, and unknown-field discovery |
| `work resume`'s `parentExecutionId` linkage — corrected, not replicated, production defect | A006 (work-lifecycle); production `transitionUnlocked`'s `resume` branch, `startExecution`/`discoverIdentity` (`tools/ros_cli.mjs`, `tools/ros_telemetry.mjs`) | manual smoke-testing before implementation revealed production's own `parentExecutionId: prior?.executionId ?? null` is dead code: `startExecution`'s `discoverIdentity(options.identity ?? options)` always picks the truthy `options.identity` sibling over the `options` object `parentExecutionId` was set on, so production's computed value is silently discarded (confirmed against real Node: always `null`). Every other increment in this migration reproduces a confirmed production quirk; this one is corrected instead, on the user's explicit instruction, since Node is being deprecated rather than patched. The F# port supplies the work item's most recently created execution as `parentExecutionId` on `resume`'s rare no-active-candidate path; every other action still supplies none, matching production's own CLI | `Ros.Infrastructure.Work.FileTelemetryQueryRepository.readLatestExecutionId` (new), `FileTelemetryExecutionRepository.CreateExecutionRequest.ParentExecutionId` (new field, threaded into the pre-existing `Identity.discover`/`IdentityInputs.ParentExecutionId`, which already worked correctly), `Ros.Cli.Program.resolveContextTelemetryWithCreation` (extended with a `parentExecutionIdFor` parameter) | typed tests cover `readLatestExecutionId` (chronological-latest resolution, none-for-unknown-item) and `createExecution` threading `ParentExecutionId` into the created record's identity (both a supplied value and the `None` default); `tests/work-resume-parent-execution-fsharp-differential.test.mjs` documents the divergence explicitly: it runs both real Node (confirming the defect: `parentExecutionId` stays `null`) and the F# CLI (confirming the corrected linkage) side by side, rather than asserting byte-for-byte parity |
| Work-attribution validation (`workFindings`) | A006; production `workFindings` in `tools/ros_cli.mjs`, one contributor to the `validate` findings pipeline | enforcement-off short-circuits before any Git observation; baseline/attributed/active-work exclusions explicit; a real Git failure is a structured finding, never a silent empty result | `Ros.Domain.Work.Attribution`, `Ros.Contracts.Work.WorkAttributionContract`, `Ros.Infrastructure.Work.FileEventLogRepository`, `FileWorkConfigRepository.readEnforceAttribution`, shadow `work validate` | typed enforcement/baseline/attribution/active-work decision tests plus event-log and config-repository unit tests; Node differential (filtering production `validate`'s findings to `field === "work_items"`) matches disabled enforcement, no meaningful change, baseline exclusion, attributed-via-event exclusion, active-work exclusion, and the unattributed-change finding; the real-Git-failure `.git` finding message format is proven by unit construction only, since neither CLI exposes a Git-executable override for an end-to-end differential |
| Execution lifecycle and telemetry | `DF-ROS-2026-A010`; metric catalog | stable closed semantics + open extensions/provenance | future `Ros.Domain.Execution/Metrics` | deferred; Node remains authority |
| Execution creation/context backlink | A010; production telemetry/work kernels | execution file atomic under per-execution lease; context mutation under work capability; retry must not duplicate evidence or guess ambiguity | `Ros.Domain.Telemetry.ExecutionLinkRecovery` and `Ros.Application.Telemetry.TelemetryOperations`; production Node composes the effect | direct and work-begin crash-shape retries adopt one detached record; ambiguity rejects and exact `--execution-id` selects; uncomposed core attachment rejects before writing |
| Provider telemetry mapping | A010 and provider adapters | open boundary, raw preservation | infrastructure adapters emitting typed observation batches | retained Node edge initially |
| Installation/profile composition | `DF-ROS-2026-A003`; profile manifests | platform adapter versus semantic initialization | future initialization/upgrade handler; npm adapter retained | deferred distribution experiment |
| Hub aggregation | `DF-ROS-2026-A009` | separate semantic area and authority | no `Ros.Domain` owner yet | deferred; spoke contract tests retained |
| GitHub validation/release | workflow and package contract | platform declarations stay at edge | future validation/release-plan commands only | deferred hosted evidence |
| Project Administration -> Time Entry | A006/A007/A009 boundary | do not invent/couple absent domain | explicit future integrations only | blocked pending authoritative requirements |

## First-slice acceptance linkage

| Acceptance | Requirement source | Planned proof |
|---|---|---|
| Node remains production authority | `DF-ROS-2026-A027` | unchanged `ros`/starter launchers and package manifest diff |
| Canonical inputs never modified | A001 | before/after hashes in isolated fixture differential case |
| Registries deterministic and equivalent | A001/A002/current behavior | Node/Python/F# fixture bytes, F# repeat build, and current-repository smoke |
| Invalid boundary data rejected explicitly | A002/SDE verification method | positive/negative frozen fixture; malformed front matter; status/reference/ID cases |
| Domain depends inward only | SDE-DOCTRINE-003/008 | project-reference architecture check plus intentional rejection |
| Public encoding deliberate | SDE-DOCTRINE-004 | golden JSON bytes and explicit writer tests |
| Partial effect is not hidden | SDE-DOCTRINE-004 | injected second-write failure yields typed `Indeterminate` incomplete outcome |
| Git unavailability is not clean | SDE boundary/verification method; work and telemetry manifests | non-repository and missing-executable tests return `Unavailable`; CLI exits 1; malformed rename record is rejected |
| Rename/copy provenance is retained | characterized porcelain-v1 `-z` contract | parser unit test retains both paths; real-repository differential matches Node/Git bytes semantically |
| Illegal work transitions are rejected | A006 and current Node transition matrix | exhaustive 16 state/action combinations compare F# decisions with the Node transition guard |
| Work obligations precede effects | current Node block/completion guards | pure tests and Node differential reject absent block reason and missing evidence types before any F# effect exists |
| Interrupted live-work persistence is recoverable | A006; MIG-05 safety acceptance | production Node holds `work-protocol`, prepares the shared journal, and recovers before transition; cross-runtime tests exercise both directions and a divergence rejection |
| Concurrent backlog changes do not lose queue entries | A006/A008; persistence safety acceptance | eight separate `ros add` processes are released concurrently; the final queue contains all eight IDs and no pending journal |
| Rollback remains available | migration non-goal/safety | Node gates remain; no authority switch or legacy deletion |

The final migration evidence record `EV-ROS-2026-A028` records the exact
commands, results, and remaining authority boundary.
