#!/usr/bin/env node

// Optional F# ROS CLI (DF-ROS-2026-A029): downloads a self-contained
// binary matching this project's ros.json rosVersion and this machine's
// platform from the upstream project's GitHub Releases on first use,
// verifies it, caches it, then execs it. Additive alongside ./ros (Node,
// unchanged) -- it is not a replacement, and command syntax differs in
// places; see docs/migrations/fsharp/STATUS.md in the upstream project
// for current command-surface parity.

import { run } from "./tools/ros_fs_launcher.mjs";

process.exitCode = await run(process.argv.slice(2));
